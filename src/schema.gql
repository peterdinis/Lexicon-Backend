# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Page {
  id: Int!
  title: String!
  isDatabase: Boolean!
  parentPageId: Int
  workspaceId: Int!
  ownerId: Int!
  sortPosition: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
  trashedAt: DateTime
  subPages: [Page!]
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Workspace {
  id: Int!
  name: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  pages: [Page!]
}

type WorkspacePaginated {
  items: [Workspace!]!
  total: Int!
}

type Notification {
  id: ID!
  userId: Int!
  type: String!
  title: String!
  description: String
  isRead: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type CalendarEvent {
  id: Int!
  title: String!
  start: DateTime!
  end: DateTime!
  pageId: Int!
  userId: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type AppModel {
  message: String!
}

type Block {
  type: String!
  content: String!
}

type Query {
  getHelloWorldList: [AppModel!]!
  getPage(id: Int!): Page!
  getPages(workspaceId: Int!, parentPageId: Int, search: String, isDatabase: Boolean, page: Int! = 1, pageSize: Int! = 10): [Page!]!
  getSubPages(parentPageId: Int!): [Page!]!
  trashedPages(workspaceId: Int!, ownerId: Int): [Page!]!
  page(id: Int!): Page!
  pageTree(workspaceId: Int!, parentPageId: Int): [Page!]!
  workspaces(query: WorkspaceQueryInput): WorkspacePaginated!
  workspace(id: Int!): Workspace!
  events(userId: Int, pageId: Int): [CalendarEvent!]!
  event(id: Int!): CalendarEvent!
  myNotifications: [Notification!]!
}

input WorkspaceQueryInput {
  search: String
  skip: Int
  take: Int
}

type Mutation {
  createPage(input: CreatePageInput!): Page!
  updatePage(id: Int!, input: UpdatePageInput!): Page!
  updatePageSortPosition(id: Int!, sortPosition: Int!): Page!
  deletePage(id: Int!): Page!
  movePageToTrash(id: Int!): Page!
  restorePageFromTrash(id: Int!): Page!
  movePage(id: Int!, newParentId: Int): Page!
  createWorkspace(input: CreateWorkspaceInput!): Workspace!
  updateWorkspace(input: UpdateWorkspaceInput!): Workspace!
  removeWorkspace(id: Int!): Workspace!
  createEvent(input: CreateEventInput!): CalendarEvent!
  updateEvent(input: UpdateEventInput!): CalendarEvent!
  deleteEvent(id: Int!): CalendarEvent!
  markNotificationRead(id: String!): Notification!
  markAllNotificationsRead: Boolean!
}

input CreatePageInput {
  title: String!
  workspaceId: Int!
  ownerId: Int!
  parentPageId: Int
  emoji: String!
  isDatabase: Boolean
}

input UpdatePageInput {
  title: String
  isDatabase: Boolean
  parentPageId: Int
}

input CreateWorkspaceInput {
  name: String!
}

input UpdateWorkspaceInput {
  name: String
  id: Int!
}

input CreateEventInput {
  title: String!
  start: DateTime!
  end: DateTime!
  userId: Int!
  pageId: Int!
}

input UpdateEventInput {
  title: String
  start: DateTime
  end: DateTime
  userId: Int
  pageId: Int
  id: Int!
}

type Subscription {
  notificationCreated: Notification!
}